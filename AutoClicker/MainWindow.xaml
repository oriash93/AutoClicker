<Window x:Class="AutoClicker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoClicker"
        xmlns:bot="clr-namespace:PixelLab.Wpf;assembly=PixelLab.Wpf"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="550">

    <Window.Resources>
        <ObjectDataProvider x:Key="mouseButtonValues"
                            MethodName="GetValues"
                            ObjectType="{x:Type system:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="local:MouseButton"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider x:Key="mouseActionValues"
                            MethodName="GetValues"
                            ObjectType="{x:Type system:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="local:MouseAction"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider x:Key="repeatModeValues"
                            MethodName="GetValues"
                            ObjectType="{x:Type system:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="local:RepeatMode"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider x:Key="LocationModeValues"
                            MethodName="GetValues"
                            ObjectType="{x:Type system:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="local:LocationMode"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <GroupBox Header="Click Interval" Grid.Row="0" Grid.ColumnSpan="2">
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Width="50"
                         Text="{Binding Hours, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
                <TextBlock Grid.Column="1" Margin="5, 0, 0, 0" Text="Hours"/>
                <TextBox Grid.Column="2" Width="50" Margin="5, 0, 0, 0"
                         Text="{Binding Minutes, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
                <TextBlock Grid.Column="3" Margin="5, 0, 0, 0" Text="Minutes"/>
                <TextBox Grid.Column="4" Width="50" Margin="5, 0, 0, 0"
                         Text="{Binding Seconds, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
                <TextBlock Grid.Column="5" Margin="5, 0, 0, 0" Text="Seconds"/>
                <TextBox Grid.Column="6" Width="50" Margin="5, 0, 0, 0"
                         Text="{Binding Milliseconds, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
                <TextBlock Grid.Column="7" Margin="5, 0, 0, 0" Text="Milliseconds"/>
            </Grid>
        </GroupBox>

        <GroupBox Header="Click Options" Grid.Row="1" Grid.ColumnSpan="2">
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Mouse Button"/>
                <ComboBox Grid.Row="1" Grid.Column="1" Margin="5, 0, 0, 5"
                          ItemsSource="{Binding Source={StaticResource mouseActionValues}}"
                          SelectedIndex="0" SelectedItem="{Binding SelectedMouseAction}"/>
            </Grid>
        </GroupBox>

        <GroupBox Header="Cursor Position" Grid.Row="2" Grid.ColumnSpan="2">
            <UniformGrid Rows="1" Columns="2"
                         HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5">
                <RadioButton Grid.Column="0" Content="Current Location" IsChecked="True"/>
                <RadioButton Grid.Column="1">
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Pick Location" Margin="5, 0, 0, 0"/>
                        <TextBlock Text="X" Margin="5, 0, 0, 0"/>
                        <TextBox Width="50" Margin="5, 0, 0, 0"/>
                        <TextBlock Text="Y" Margin="5, 0, 0, 0"/>
                        <TextBox Width="50" Margin="5, 0, 0, 0"/>
                    </StackPanel>
                </RadioButton>
            </UniformGrid>
        </GroupBox>

        <Button Grid.Row="3" Grid.ColumnSpan="2" Content="click" Click="Button_Click"/>
    </Grid>
</Window>
